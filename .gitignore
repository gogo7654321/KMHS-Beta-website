# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
/node_modules

# Build artifacts
/.next/
/out/
/build/

# Local Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase web config (src/firebase/config.ts) is committed intentionally.
# These are public client-side identifiers, not secrets — access is enforced
# by Firestore/Storage security rules. Required at build time by App Hosting.

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts
.vercel
