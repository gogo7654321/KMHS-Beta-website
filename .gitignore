# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
/node_modules

# Build artifacts
/.next/
/out/
/build/

# Local Environment Variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# src/firebase/config.ts is committed but contains NO keys — it only reads the
# Firebase config from NEXT_PUBLIC_FIREBASE_CONFIG (see .env.example). Provide
# the value via .env.local locally and a Secret Manager secret on App Hosting.

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts
.vercel
